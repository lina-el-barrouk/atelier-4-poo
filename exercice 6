#include <iostream>
using namespace std;

class Point {
private:
    float x, y;
public:
    Point(float x = 0, float y = 0) : x(x), y(y) {}
    void deplace(float dx, float dy) { x += dx; y += dy; }
    void affiche() const { cout << "(" << x << ", " << y << ")" << endl; }
};
